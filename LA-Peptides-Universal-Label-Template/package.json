{
  "name": "la-peptides-universal-label-template",
  "version": "1.0.0",
  "private": true,
  "description": "Portable, brand-neutral, theme-driven vial-label template (1.77in x 0.77in, three-section layout). Build the master deliverables or a specific brand partner's finished label from master-label.svg + a theme JSON + (optionally) a field-map JSON.",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:master": "node scripts/build-master.js",
    "build:example-brand": "node scripts/build.js --theme example-brand-theme.json --out .build/example-brand --zip"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.10",
    "archiver": "^7.0.1",
    "pdf-lib": "^1.17.1",
    "pdfkit": "^0.15.1",
    "qrcode": "^1.5.4",
    "sharp": "^0.33.5",
    "svg-to-pdfkit": "^0.1.8",
    "xpath": "^0.0.34"
  }
}
